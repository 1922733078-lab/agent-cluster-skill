interface:
  display_name: "agent集群"
  short_description: "Delegate via dynamic agent teams"
  default_prompt: "Use $agent-cluster to act as an orchestrator-only main agent: delegate substantive work to dynamically spawned subagents, route follow-ups by dependency, and synthesize the final outcome."
