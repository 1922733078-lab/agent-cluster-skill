interface:
  display_name: "agent集群"
  short_description: "Coordinate dependency-aware agents"
  default_prompt: "Use $agent-cluster to coordinate a dependency-aware iterative agent team for this complex task, continue branches as soon as required results return, and integrate the final outcome."
